import { OmitProps } from '../types';
export declare const allowed: ({ inHeader, columnKey, headers, forwardedRef, ...rest }: OmitProps) => {
    className?: string;
    colSpan?: number;
    children?: import("react").ReactNode;
};
export default allowed;
